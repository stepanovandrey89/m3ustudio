# Copy to scripts/.deploy.env and fill in your production host.
# scripts/.deploy.env is gitignored — this file is not.

DEPLOY_HOST=root@YOUR_SERVER_IP
# Optional overrides:
# DEPLOY_DIR=/opt/m3uplaylist
# DEPLOY_USER=m3ustudio
